# Copie para .env ao desenvolver a landing localmente (vite.spa.config.ts)
# Produção: não commitar .env — o build usa /api e financeiro.fluxiva.app por padrão.

VITE_CENTERFLOW_API_URL=http://localhost:3001/api
VITE_CENTERFLOW_APP_URL=http://localhost:5173
