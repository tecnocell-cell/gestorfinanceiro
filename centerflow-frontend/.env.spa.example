# CenterFlow landing SPA — copie para .env.spa.local e ajuste

# URL base da API (sem barra final; /auth/* é acrescentado pelo client)
VITE_CENTERFLOW_API_URL=http://localhost:3001/api

# App autenticado (Gestor) — redirecionamento após login/cadastro
VITE_CENTERFLOW_APP_URL=http://localhost:5173
